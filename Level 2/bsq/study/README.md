Exercise Goal

-Build the biggest square on map given. File will have 4 info and map. if no map, take from stdin.
	the 4 info
	-empty char, 
	-fill char, 
	-obstacle char, 
	-no of lines
-must be able to take several maps. print map result, print \n, next map result.
-map must comply to the rules given
-if needed, print "Error: " followed by message

BSG Algorithm main
	-Load elements
	-Load maps
	-Find big square
